{
  "name": "hushpot-deploy-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "_comment": "Scripts-only manifest. ALL runtime dependencies live in the ROOT package.json (single canonical tree). Never `npm install` in this directory - that recreates the duplicate-tree class-split bug that broke deploy attempt 3.",
  "scripts": {
    "address": "tsx src/main.ts address",
    "run": "tsx src/main.ts run",
    "hushpot:address": "tsx src/hushpot-main.ts address",
    "hushpot:deploy": "tsx src/hushpot-main.ts deploy",
    "hushpot:demo": "tsx src/hushpot-main.ts demo",
    "hushpot:lifecycle": "tsx src/hushpot-main.ts lifecycle"
  }
}
